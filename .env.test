DATABASE_URL=postgres://testuser:testpass@localhost:5432/testdb
POSTGRES_HOST=localhost    # Or the appropriate host for your test DB
POSTGRES_PORT=5432        # Or the port your test DB is running on
POSTGRES_USER=testuser    # Replace with the correct test user
POSTGRES_PASSWORD=testpass  # Replace with the correct test password
POSTGRES_DB=testdb        # The name of your test database

